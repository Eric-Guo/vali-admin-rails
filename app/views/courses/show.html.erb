<main class="app-content">
  <div class="app-title">
    <div>
      <h1><i class="fa fa-laptop"></i> <%= @course.title -%></h1>
      <p></p>
    </div>
    <ul class="app-breadcrumb breadcrumb">
      <li class="breadcrumb-item"><i class="fa fa-home fa-lg"></i></li>
      <li class="breadcrumb-item"><a href="<%= courses_path -%>"><%= t('ui.course_list') -%></a></li>
      <li class="breadcrumb-item"><a href="<%= course_path(@course) -%>"><%= t('ui.course_detail') -%></a></li>
    </ul>
  </div>
  <div class="row">
    <div class="col-md-12">
      <div class="tile">
        <h3 class="tile-title">
          <span class="badge badge-info"><%= t('course.status.title') -%>: <%= @course.status -%></span>
          <span class="badge badge-secondary"><%= t('course.rsvp_count') -%>: <%= @course_users.count -%></span>
        </h3>
        <div class="tile-body">
          <h5>
            <% @course.vertical_markets.each do |vm| -%>
              <span class="badge badge-primary"><%= vm.name -%></span>
            <% end -%>
          </h5>
          <p><%= @course.address -%></p>
          <br />
          <%= simple_format @course.body -%>
          <form class="form-horizontal">
            <div class="form-group row">
              <label class="control-label col-md-3"><%= t('course.company') -%></label>
              <div class="col-md-8">
                <%= select_tag :company_id, options_from_collection_for_select(policy_scope(Company), 'id', 'name'),
                  class: 'form-control col-md-8', id: 'to-filter-company-id', include_blank: true,
                  'data-remote' => true, 'data-type' => 'script',
                  'data-url' => url_for(controller: 'courses', action: 'select_company_changed', format: 'js') %>
              </div>
            </div>
          </form>
          <%= render partial: 'add_user_to_table', locals: { course: @course, users: @users } %>
          <%= render partial: 'course_trainees_table', locals: { course: @course, users: @course_users } %>
        </div>
      </div>
    </div>
  </div>
</main>
